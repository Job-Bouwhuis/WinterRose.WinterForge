// #import "human2.txt" (compiles into opcodes2.wfbin)
// #import "human3.txt" (compiles into opcodes3.wfbin)

#container Bor
{
	#template Factorial int n
	{
		if n <= 1
		{
			return 1;
		}
		else
		{
			return n * Factorial(n - 1);
		}
	}
}
Bor : b;
var fac = b->Factorial(5);

Console->WriteLine(fac);

return ba;