//#import "human2.wf" // works but commented out because these files were removed from the repo to reduce clutter
//#import "human3.wf"

for var i = 0; i < 10; i = i + 1;
{
	Console->WriteLine(i);
}


isThisCool = true;
if isThisCool // blocks can be written without braces if they only contain one statement
	Console->WriteLine("yes");
else
	Console->WriteLine("no");

while true // here too could braces be removed.
{
	Console->WriteLine("infinite loooooooooooooooop");
}


#container Foo
{
	variables
	{
		x;
		y = 5;
		z = Math->Sin(Time->deltaTime);
	}

	#template ThisIsAFunction
	{
	}

	#template ThisOneHasArgs int num, string a, Template callback
	{
		// yes templates can be passed around as arguments and stored in variables
		
		// templates and entire container definitions can be nested inside other containers or templates
		#template inner
		{
		}

		#container InnerContainer
		{
			#template templateSeption
			{
			}
		}

		// order of definition and usage doesnt matter
		InnerContainer : ic; // create instance of nested conatiner
		return ic; // return instance
	}
}